// git init -> transforma a pasta em um repositorio git
// git status -> Mostra status do repositorio 
// git add <nome do arquivo> -< adiciona o arquivo pra ser comitado
// git commit -m "Mensagem de commit necessaria" Salva mudanca com comentario

// git remote add origin https://github.com/seuusuario/meu-projeto.git -> conecta seu repositório local ao GitHub.
// git branch -M main -> garante que a branch principal seja "main".
// git push -u origin main -> envia os arquivos para o GitHub.

// git config --global user.name "Seu Nome"
// git config --global user.email "seu@email.com" <- Isso configura seu nome e email para que o Git saiba quem está fazendo as mudanças.

/*
Dicas e boas práticas 

✅ Faça commits pequenos e descritivos
✅ Use branches para testar novas funcionalidades
✅ Antes de começar, sempre rode git pull
✅ Nunca suba arquivos secretos (como senhas)
*/

// git clone https://github.com/seuusuario/meu-projeto.git -> clonar um repositorio
// cd meu-projeto -> entrar na pasta do repositorio